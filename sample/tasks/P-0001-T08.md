---
ref: P-0001-T08
project_ref: P-0001
task_type: execute
task_class: code-task
state: approved
brief: Classification, gate enforcement, presentation mode, and P-0001 scored against itself
approved_by: null
approved_at: null
est_p95_usd: 14.0
---

Copied from `tasks/P-0001-T08.md` for the sample (RECKON-1.1-SPEC.md decision 3): state changed
from `shipped` to `approved` so the real gate decides it instead of blocking it, and every field
naming a person is cleared. This copy carries only what the gate reads; the full history is the
register's own file, not shipped here.
