---
ref: P-0001-T09
project_ref: P-0001
task_type: design
task_class: investigation
state: approved
brief: Does a scripted -p run stay on the subscription rail (§5.3)?
approved_by: null
approved_at: null
est_p95_usd: 4.0
---

Copied from `tasks/P-0001-T09.md` for the sample: state changed
from `shipped` to `approved` so the real gate decides it instead of blocking it, and every field
naming a person is cleared. This copy carries only what the gate reads; the full history is the
register's own file, not shipped here.
